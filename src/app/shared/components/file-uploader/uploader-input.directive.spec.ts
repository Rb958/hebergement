import { UploaderInputDirective } from './uploader-input.directive';

describe('UploaderInputDirective', () => {
  it('should create an instance', () => {
    const directive = new UploaderInputDirective();
    expect(directive).toBeTruthy();
  });
});
